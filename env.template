# ============================================
# MongoDB Database Connections
# ============================================

# Main Database URI (Production Database)
MONGODB_URI=mongodb+srv://hetjani818_db_user:123@cluster0.ux8dqnc.mongodb.net/myapp?appName=Cluster0

# Archive Database URI (For Historical Data)
ARCHIVE_MONGODB_URI=mongodb+srv://hetjani818_db_user:123@cluster0.6s5idrd.mongodb.net/archive?appName=Cluster0

# ============================================
# Server Configuration
# ============================================

# Server Port (default: 3000)
PORT=3000

# ============================================
# JWT Authentication
# ============================================

# JWT Secret Key (CHANGE THIS IN PRODUCTION!)
# Generate a strong random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# Cloudinary Configuration (Image/File Upload)
# ============================================

# Cloudinary Cloud Name
CLOUDINARY_CLOUD_NAME=drsz9dqpo

# Cloudinary API Key
CLOUDINARY_API_KEY=459677147625943

# Cloudinary API Secret
CLOUDINARY_API_SECRET=i8PWcWKwuK0GTM49kjdFc-ICzu0

# ============================================
# Business Configuration
# ============================================

# Supplier GSTIN (for invoice generation)
# Format: 24AABCU9603R1ZX (2 digits state code + 10 digits PAN + 1 digit entity number + 1 letter Z + 1 digit check digit)
SUPPLIER_GSTIN=24AABCU9603R1ZX

# Default UPI ID (for admin settings)
DEFAULT_UPI_ID=your-upi-id@paytm

# ============================================
# Notes:
# ============================================
# 1. Copy this file to .env: cp env.template .env
# 2. Never commit .env to version control (it's in .gitignore)
# 3. Change JWT_SECRET to a strong random string in production
# 4. Update Cloudinary credentials if you have your own account
# 5. Update SUPPLIER_GSTIN with your actual GSTIN
# 6. Update DEFAULT_UPI_ID with your actual UPI ID

